import Add from '@mui/icons-material/Add';
import { Button } from '@mui/material';

const WorkoutCreator = () => {
  return <Button startIcon={<Add />}>Create new workout</Button>;
};

export default WorkoutCreator;
