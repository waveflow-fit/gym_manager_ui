import Add from '@mui/icons-material/Add';
import { Button } from '@mui/material';

const DietCreator = () => {
  return <Button startIcon={<Add />}>Create new diet</Button>;
};

export default DietCreator;
